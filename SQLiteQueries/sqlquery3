SELECT 
    g.game,
    COUNT(tm.match_id) AS matches_played,
    AVG(tm.score) AS avg_score
FROM game g
JOIN tournament_matches tm ON g.game_id = tm.game_id
GROUP BY g.game_id, g.game;

# Game Stats Summary
